events { worker_connections 1024; }

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;
    server_tokens off;
    resolver 127.0.0.11 ipv6=off valid=30s;
    include /etc/nginx/conf.d/*.conf;

    # HTTP: API + Web（若已申请证书，可改为 301 跳转到 HTTPS）
    server {
        listen 80;
        server_name _;

        # 证书签发时给 certbot 用
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        # API 代理（含 SSE 支持）
        location /bobo/ {
            proxy_pass         $bobo_backend;
            proxy_http_version 1.1;
            proxy_set_header   Host $host;
            proxy_set_header   X-Real-IP $remote_addr;
            proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header   X-Forwarded-Proto $scheme;
            proxy_set_header   X-Request-Id $http_x_request_id;

            proxy_set_header   Connection '';
            proxy_buffering    off;
            proxy_cache        off;
            proxy_read_timeout 600s;
            chunked_transfer_encoding on;
        }

        # PC 端静态文件（web/dist）
        location / {
            root  /usr/share/nginx/html;
            try_files $uri $uri/ /index.html;
            expires 7d;
            add_header Cache-Control "public, immutable";
            add_header X-Content-Type-Options nosniff always;
            add_header X-Frame-Options DENY always;
            add_header Referrer-Policy "strict-origin-when-cross-origin" always;
        }
    }

    # HTTPS 模板（你有域名+证书后打开）
    # server {
    #     listen 443 ssl;
    #     server_name bobo.yourdomain.com;
    #
    #     ssl_certificate     /etc/letsencrypt/live/bobo.yourdomain.com/fullchain.pem;
    #     ssl_certificate_key /etc/letsencrypt/live/bobo.yourdomain.com/privkey.pem;
    #     ssl_protocols       TLSv1.2 TLSv1.3;
    #
    #     location /bobo/ {
    #         proxy_pass         $bobo_backend;
    #         proxy_http_version 1.1;
    #         proxy_set_header   Host $host;
    #         proxy_set_header   X-Real-IP $remote_addr;
    #         proxy_set_header   X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header   X-Forwarded-Proto $scheme;
    #         proxy_set_header   X-Request-Id $http_x_request_id;
    #         proxy_set_header   Connection '';
    #         proxy_buffering    off;
    #         proxy_cache        off;
    #         proxy_read_timeout 600s;
    #         chunked_transfer_encoding on;
    #     }
    #
    #     location / {
    #         root  /usr/share/nginx/html;
    #         try_files $uri $uri/ /index.html;
    #         expires 7d;
    #         add_header Cache-Control "public, immutable";
    #         add_header X-Content-Type-Options nosniff always;
    #         add_header X-Frame-Options DENY always;
    #         add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    #     }
    # }
}
